{
  "name": "app-tech-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "*",
    "typescript": "^5.6.0",
    "wrangler": "^4.106.0"
  }
}
